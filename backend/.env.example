# ── Database ──────────────────────────────────────────
# SQLite for dev, PostgreSQL for production:
# DATABASE_URL=postgresql+psycopg://constructerp:constructerp@localhost:5432/constructerp
DATABASE_URL=sqlite:///./constructerp.db

# ── Auth ──────────────────────────────────────────────
# Generate with: python -c 'import secrets;print(secrets.token_hex(32))'
SECRET_KEY=
# CORS_ORIGINS=http://localhost:3000,https://app.constructerp.dev
CORS_ORIGINS=*

# ── File Storage ──────────────────────────────────────
STORAGE_BACKEND=local
UPLOAD_DIR=./uploads
MAX_UPLOAD_SIZE_MB=50
# S3 / MinIO:
# S3_BUCKET=constructerp
# S3_KEY=minioadmin
# S3_SECRET=minioadmin
# S3_REGION=us-east-1
# S3_ENDPOINT=http://localhost:9000

# ── AI Agent ──────────────────────────────────────────
# AI_PROVIDER=openai | ollama | disabled
AI_PROVIDER=disabled
# OPENAI_API_KEY=sk-...
# OLLAMA_BASE_URL=http://localhost:11434
# OLLAMA_MODEL=llama3.2

# ── App ───────────────────────────────────────────────
SEED_DEMO_USERS=true